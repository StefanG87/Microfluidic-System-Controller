# Historical GUI Plan

This file is kept only as historical context for the staged GUI build-up that started the project.

It is not the current architecture reference. Use these files instead:

- `README.md` for project overview and local usage.
- `ARCHITECTURE.md` for module boundaries and automation contract details.
- `TESTING.md` for manual validation on the lab setup.

The current implementation has grown beyond the original staged plan and now includes hardware profiles, Fluigent sensors, rotary-valve support, measurement sessions, CSV export, and JSON automation programs.