# Python cache and virtual envs
__pycache__/
*.py[cod]
*.pyo
*.pyd
.venv/
.venv-v3/
venv/

# Build artifacts
build/
dist/
editor/dist/
*.spec.bak

# Runtime outputs
Measurements/
*.log

# Local preferences and machine-specific state
# pressure_offset.json also stores v3 plot-channel preferences.
# device_prefs.json stores v3 hardware profile, program favorites, rotary COM,
# CSV export prefix, and optional read-timing export settings.
lookup/device_prefs.json
lookup/pressure_offset.json
editor/lookup/device_prefs.json
editor/lookup/pressure_offset.json

# OS/editor noise
Thumbs.db
.DS_Store
*.tmp
*.temp

# Optional test and coverage output
.pytest_cache/
.coverage
htmlcov/

